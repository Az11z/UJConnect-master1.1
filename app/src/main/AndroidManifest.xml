<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.customer.ujconnect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" >

        </activity>
        <activity
            android:name=".DepartmentsListActivity"
            android:label="@string/title_activity_departments_list"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DepartmentPageActivity"
            android:label="@string/title_activity_department_page"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".CreateNewsActivity">




        </activity>
    </application>

</manifest>